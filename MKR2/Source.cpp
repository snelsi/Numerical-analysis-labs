#include "Matrix.h"	// Бiблiотека роботи з матрицями
#include <iostream>

using namespace std;


int main()
{
	Matrix matrix1(3);
	Matrix matrix2(4);
	Matrix product = matrix1 * matrix2;
	matrix.init();
	matrix.print();
	system("pause");
	return 0;
}
